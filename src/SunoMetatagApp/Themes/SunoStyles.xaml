<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="SunoTokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- ====================================================================
         Suno styles — implicit + named styles consuming SunoTokens.xaml.
         Reference: docs/specs/2026-05-26-suno-visual-redesign.md §3, §4, §5
         ==================================================================== -->

    <!-- ====================================================================
         §3.2 Implicit styles (no x:Key)
         ==================================================================== -->

    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource SunoFontPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeBody}" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource SunoFontPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeBody}" />
    </Style>

    <Style TargetType="{x:Type GridSplitter}">
        <Setter Property="Background" Value="{StaticResource SunoBorderStrongBrush}" />
    </Style>

    <!-- ====================================================================
         §3.1 + §4 Named styles
         ==================================================================== -->

    <!-- §4 base: SunoButton (general-purpose dark button) -->
    <Style x:Key="SunoButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceCardElevatedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingControl}" />
        <Setter Property="FontFamily" Value="{StaticResource SunoFontPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeBody}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource SunoRadiusM}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="Content"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontFamily="{TemplateBinding FontFamily}"
                                          TextElement.FontSize="{TemplateBinding FontSize}"
                                          TextElement.FontWeight="{TemplateBinding FontWeight}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoBorderStrongBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoBorderSubtleBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessFocused}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource SunoTextDisabledBrush}" />
                <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- §4.2 SunoIconButton: compact toolbar (▲ ▼ ×) -->
    <Style x:Key="SunoIconButton" TargetType="{x:Type Button}" BasedOn="{StaticResource SunoButton}">
        <Setter Property="Background" Value="{StaticResource SunoTransparentBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoTransparentBrush}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingIconButton}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeSmall}" />
        <Setter Property="MinWidth" Value="28" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoSurfaceCardElevatedBrush}" />
                <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoBorderSubtleBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource SunoTextDisabledBrush}" />
                <Setter Property="Background" Value="{StaticResource SunoTransparentBrush}" />
                <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- §4.8 SunoPrimaryButton: Copy all + Add section (accent-filled) -->
    <Style x:Key="SunoPrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource SunoButton}">
        <Setter Property="Background" Value="{StaticResource SunoAccentPrimaryBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoAccentPrimaryTextBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryTextBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoBorderStrongBrush}" />
                <Setter Property="Foreground" Value="{StaticResource SunoAccentPrimaryTextBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource SunoBorderStrongBrush}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryTextBrush}" />
                <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessFocused}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource SunoBorderSubtleBrush}" />
                <Setter Property="Foreground" Value="{StaticResource SunoTextDisabledBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
                <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        §4.4 SunoTagPill: tag-picker pill. CRITICAL: x:Key="SunoTagPill" must
        exist for the picker-scoped ScrollViewer.Resources Style to BasedOn
        resolve. If this key is renamed or removed, picker-scoped focus +
        opacity discipline silently breaks and the focus-flip-stale-insert
        risk (see wiki risks/focus-flip-stale-insert.md) re-emerges.
    -->
    <Style x:Key="SunoTagPill" TargetType="{x:Type Button}" BasedOn="{StaticResource SunoButton}">
        <!-- Pinkish-purple (fuchsia) default fill per user direction 2026-05-26.
             Was dark + accent-tint hover; now accent-filled by default with
             lighter hover and deeper pressed states. -->
        <Setter Property="Background" Value="{StaticResource SunoAccentPillBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPillBrush}" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeSmall}" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource SunoRadiusPill}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontFamily="{TemplateBinding FontFamily}"
                                          TextElement.FontSize="{TemplateBinding FontSize}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoAccentPillHoverBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPillHoverBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SunoAccentPillPressedBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPillPressedBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- §4.3 / §4.6 SunoTextBox: search box + base for lyric -->
    <Style x:Key="SunoTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingControl}" />
        <Setter Property="FontFamily" Value="{StaticResource SunoFontPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeBody}" />
        <Setter Property="CaretBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
        <Setter Property="SelectionBrush" Value="{StaticResource SunoAccentPrimarySoftBrush}" />
        <Setter Property="SelectionOpacity" Value="0.6" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource SunoRadiusS}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      Background="Transparent"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                      VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessFocused}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- §4.3 SunoLyricTextBox: lyric edit textbox (monospace + multiline) -->
    <Style x:Key="SunoLyricTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource SunoTextBox}">
        <Setter Property="FontFamily" Value="{StaticResource SunoFontMono}" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="MinLines" Value="6" />
        <Setter Property="MaxLines" Value="14" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <!-- §4.6 SunoComboBox: category dropdown -->
    <Style x:Key="SunoComboBox" TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SunoTextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingControl}" />
        <Setter Property="FontFamily" Value="{StaticResource SunoFontPrimary}" />
        <Setter Property="FontSize" Value="{StaticResource SunoFontSizeBody}" />
        <!-- Note: full ComboBox template override is out of scope for v1.2.
             Default Aero5 popup chrome may render lighter than the app theme.
             Acceptable v1.2 visual gap; see spec §7 out-of-scope notes. -->
    </Style>

    <!-- §4.1 SunoSectionCard: section card Border -->
    <Style x:Key="SunoSectionCard" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceCardBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="CornerRadius" Value="{StaticResource SunoRadiusL}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingCard}" />
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource SunoAccentPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessFocused}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- §4.5 SunoPreviewCard: preview pane Border -->
    <Style x:Key="SunoPreviewCard" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource SunoSurfaceCardBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoBorderSubtleBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="CornerRadius" Value="{StaticResource SunoRadiusL}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingCard}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!-- §4.7 SunoErrorBanner: error banner Border -->
    <Style x:Key="SunoErrorBanner" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource SunoErrorBgBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SunoErrorTextBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource SunoBorderThicknessThin}" />
        <Setter Property="CornerRadius" Value="{StaticResource SunoRadiusS}" />
        <Setter Property="Padding" Value="{StaticResource SunoPaddingControl}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

</ResourceDictionary>
